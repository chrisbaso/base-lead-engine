This is a template, not a live advisor site.
Smart Retirement MN is reference content for the base-lead-engine chassis. The advisor profile reflects credentials held by Chris Baso (CFP®, CLU®, RICP®, Series 65, Series 7) but does not represent an active investment advisory firm. Smart Retirement MN is not currently registered as an investment adviser in any state. This template represents the future-state configuration the firm would adopt at the point of registering as an RIA.
Do not deploy this tenant configuration to a public domain without first completing IAR registration under a state-registered or SEC-registered RIA, replacing all CRD/IARD numbers with current values, and submitting all marketing content for compliance review.

Deploy blockers:
- `crdNumber` is set to `9999999` so schema validation can pass. Replace it with the current CRD/IARD number before any public deploy.
- `brokerCheckUrl`, `adv2Url`, and `privacyPolicyUrl` are placeholder URLs.
- `schedulingUrl` is a placeholder Calendly URL.
- `ownerEmail` and `fromAddress` are placeholder email addresses.
