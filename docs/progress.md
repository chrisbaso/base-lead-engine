# Progress

## Phase 1 - Foundation

Built:
- Created the pnpm workspace and Turborepo task graph.
- Added `apps/web` and `apps/admin` as Next.js App Router apps.
- Added strict TypeScript, ESLint, Vitest, and CI wiring.
- Added `packages/core`, `packages/db`, `packages/ui`, tenant schema, and the `demo` tenant.
- Added the initial Supabase migration with tenant-scoped tables and RLS policies.
- Added hostname-to-tenant resolution and runtime tenant config validation.

Skipped:
- Live Supabase verification because no project credentials are present in this fresh workspace.
- Full Clerk auth UI in the admin app; Phase 1 only wires the app shell and dependency.

Risks:
- RLS policies assume JWTs include a `tenant_id` claim. Admin/service workflows will need explicit service-role paths plus application-level authorization.
- Next.js is pinned to the latest patched 15.x line to honor the requested stack while avoiding known App Router security issues.

Review when back:
- Confirm the tenant JWT claim strategy before production Supabase policies are frozen.
- Confirm whether admin reads should use a service role with Clerk email allowlisting or tenant-scoped user claims.

## Phase 2 - Lead Capture + Tracking

Built:
- Added schema-driven lead capture primitives for quiz, calculator, multi-step form, and single form flows.
- Updated the demo tenant to a working three-step quiz with partial-save email capture and localStorage persistence.
- Added a Server Action submission path that validates tenant-defined fields, upserts Supabase leads, records lead events, and emits downstream tracking.
- Added tracking config and fanout for GTM, Meta CAPI, Google Enhanced Conversions endpoint delivery, plus client pixel fallbacks.
- Added tests for tenant field validation, tracking config exposure, and the lead submission pipeline.

Skipped:
- Live Supabase, Meta, and Google API verification because real credentials are not present in the workspace.

Risks:
- Google Enhanced Conversions is represented as a tenant-configurable server endpoint until the production Google Ads OAuth/service account strategy is confirmed.
- Demo partial-save currently upserts by tenant and email once email exists; anonymous pre-email partials are not persisted.

Review when back:
- Provide staging Supabase, Meta access token, and Google Ads server-side conversion credentials.
- Confirm whether anonymous partial submissions should create leads before email capture.

## Phase 3 - Email Engine

Built:
- Added tenant-configured email sequence metadata with subject, from-name, conditions, and delay hours.
- Added the demo tenant's five-email sequence.
- Added email scheduling on completed lead submission, including qualified/nurture condition filtering.
- Added Vercel cron route for due email sends through Resend's REST API.
- Added unsubscribe page, suppression table, and Resend webhook handling for opened, clicked, and bounced events.
- Added email rendering tests and scheduling tests.

Skipped:
- Live Resend delivery verification because `RESEND_API_KEY` is not present in the workspace.
- Domain deliverability verification because tenant email domains are not connected yet.

Risks:
- Email HTML is generated by a runtime-safe template renderer rather than `react-dom/server`, because Next.js rejects `react-dom/server` imports through Server Action reachable modules. The template layer is isolated so it can later be moved to a dedicated React Email worker if needed.
- Unsubscribe links are signed when `UNSUBSCRIBE_SIGNING_SECRET` is configured. Local development without the secret keeps legacy links valid.

Review when back:
- Add a staging Resend API key and verify demo sequence delivery, webhook events, and suppression behavior.
- Set `UNSUBSCRIBE_SIGNING_SECRET` before first production email launch.

## Phase 4 - CRM Sync

Built:
- Added `CRMAdapter` interface with HubSpot implementation and Salesforce/GoHighLevel stubs.
- Added CRM sync enqueueing after completed lead capture.
- Added retry/dead-letter behavior in `crm_sync_log` with exponential backoff.
- Added Vercel cron route for queued CRM sync processing.
- Added demo tenant HubSpot mapping fields, pipeline/stage placeholders, and list placeholder.
- Added unit coverage for CRM sync enqueueing and updated the lead pipeline test to include CRM scheduling.

Skipped:
- Live HubSpot contact/deal/list verification because no HubSpot private app token is present in the workspace.

Risks:
- HubSpot association type IDs are set to common HubSpot-defined defaults for contact-to-deal and note-to-contact. These should be verified against the target portal before production use.
- Demo HubSpot pipeline, stage, and list IDs are placeholders until a test account is connected.

Review when back:
- Provide a staging/test HubSpot private app token and confirm pipeline, stage, list, and custom property IDs.
- Run a full staging lead lifecycle after Supabase migrations are applied.

## Phase 5 - Scoring + Admin Dashboard

Built:
- Added tenant-configured scoring rules with reasons and deterministic score calculation.
- Wired scoring into lead submission, including score persistence, sequence branching, CRM enqueueing, and high-score notification triggers.
- Added Slack webhook and Resend email notification support for high-score leads.
- Added a Clerk-protected admin dashboard with an email allowlist from `CLERK_ADMIN_EMAIL`.
- Added admin pages for leads, funnel step metrics, email sequences, and read-only tenant config viewing.
- Replaced static admin demo data with Supabase-backed loaders for overview metrics, lead tables, timelines, funnel attribution, and email delivery health.
- Added unit coverage for the scoring engine and re-ran the full typecheck, lint, test, and build gates.

Skipped:
- Live admin database reads because staging Supabase credentials are not present in the workspace.
- Live Slack and notification email verification because webhook and Resend credentials are not present.

Risks:
- Admin pages show graceful unavailable/empty states until the staging Supabase project is connected.
- Manual score override UI is not persisted yet; the scoring engine and lead score persistence are ready for the DB-backed admin action.

Review when back:
- Provide staging Supabase credentials and validate the admin dashboard against real tenant, lead, event, email, and CRM rows.
- Confirm the final admin allowlist email and Clerk production keys before deploying the admin app.

## Phase 6 - Hardening

Built:
- Added shared structured logging with tenant-aware JSON log entries.
- Added error boundaries for both the funnel app and admin dashboard.
- Added rate limiting on lead submission with Upstash Redis REST support and an in-memory local fallback.
- Added Cloudflare Turnstile verification for completed lead submissions when Turnstile credentials are configured.
- Added optional Sentry-compatible exception capture through `SENTRY_DSN`.
- Added a shared retry wrapper and applied it to external HTTP calls for tracking, email, CRM, notifications, bot verification, rate limiting, and error tracking.
- Added environment placeholders for Upstash, Turnstile, Sentry, and Slack.

Skipped:
- Live Upstash, Turnstile, Sentry, and Slack verification because credentials are not present in the workspace.

Risks:
- The local in-memory rate limiter is only a development fallback; production must configure Upstash Redis REST credentials.
- Sentry capture uses direct envelope ingestion to avoid adding an SDK while credentials are absent. If richer source maps/session context are needed, replace it with `@sentry/nextjs`.

Review when back:
- Configure `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY`, `SENTRY_DSN`, and `SLACK_WEBHOOK_URL` in staging.
- Run a staged lead submit under rate-limit and Turnstile-positive/negative cases before production traffic.

## Phase 7 - Documentation

Built:
- Added `docs/architecture.md` with system diagrams, tenant model, lead lifecycle, and core decisions.
- Added `docs/launch-new-tenant.md` with the step-by-step tenant launch playbook.
- Added `docs/operations.md` with runbooks for stuck email sends, failed CRM syncs, tracking discrepancies, bot protection, admin access, and logs.
- Expanded the root README with quickstart, verification, docs index, and tenant deployment notes.

Skipped:
- Screenshots and live service examples because the staging services are not connected yet.

Risks:
- The launch playbook assumes the tenant registry remains code-based; if tenant discovery moves fully into DB later, update the registry step.

Review when back:
- Walk through the launch playbook with a real second tenant and tighten any timing or credential steps.

## Phase 8 - Retirement Funnel Migration Prep

Built:
- Read the Phase 8 migration instructions in `reference/retirement-funnel/README-for-codex.md`.
- Checked the reference folder for the actual Replit source.
- Extracted the pasted retirement calculator component into `packages/tenants/retirement/`.
- Added retirement copy, lead-capture definition, scoring rules, empty sequence placeholder, staging-safe tenant config, and email-template README.
- Added tenant-field validation metadata for retirement age, savings, state, contact fields, and helper text.
- Added `docs/migration-checklist.md` with cutover and rollback steps.

Skipped:
- Email template conversion because `/api/send-snapshot-email` and Resend templates were not provided.
- CRM/Zapier migration because no CRM or zap source was provided.
- Production tracking IDs because no GTM, Meta Pixel, Google Ads conversion IDs, or labels were provided.
- Deployment and traffic switch by explicit instruction.

Risks:
- The migration is based on pasted chat source, not a checked-in `/reference/retirement-funnel/` source tree.
- Footer disclosures are referenced but not visible in the pasted component because `Footer` is imported from another file.
- The platform lead-capture primitive does not yet exactly reproduce the original gated results UX; the tenant config preserves fields and copy, while custom rendering parity remains a migration task.

Review when back:
- Add the actual retirement funnel source files to `reference/retirement-funnel/` next to `README-for-codex.md`.
- Review `packages/tenants/retirement/` against source, especially disclosures, email templates, tracking IDs, and CRM mappings.
- Do not deploy or cut over the retirement tenant until the extracted config and checklist are reviewed.

## Defaults Approval Update

Built:
- Recorded owner approval to use recommended defaults for all remaining reversible decisions.
- Moved resolved defaults out of `docs/decisions-needed.md` and into `docs/architecture.md` as settled implementation decisions.

Risks:
- `docs/decisions-needed.md` now intentionally focuses on environment blockers, missing credentials/source, and retirement migration review items.
